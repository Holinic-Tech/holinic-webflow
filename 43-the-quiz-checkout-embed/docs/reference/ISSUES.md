# Phase 0 Issues & Discrepancies

Running log of wrong-direction signals, source disagreements, and open dependencies found while building the golden reference.

| # | Source A | Source B | Discrepancy | Resolution | Status |
|---|----------|----------|-------------|------------|--------|
| 1 | Owner hypothesis: server-side Converge handled by Make.com | Worker source `QuizSubmissions/src/queue/quizProcessor.ts:121` + `src/services/converge.service.ts:5,36-95` | Worker fires Converge `Signed Up` DIRECTLY to `https://app.runconverge.com/api/webhooks` — Make is NOT in the Converge path (outcome A) | Documented in tracking-contract.md as (A). Rebuild replicates the call TO the Worker, not the Converge call; client must NOT re-fire `Signed Up` | RESOLVED |
| 2 | Phase 0 code analysis | Owner (authoritative on ad infra) | Owner confirms the FULL Converge event set = `quiz started`, `quiz completed`, `signed up`, + `$page_load`. Code shows the client does NOT directly `cvg` quiz started/completed → they reach Converge via a **GTM relay** of the GA dataLayer events (GTM-TT5MJDF config, not in app code). Rule for rebuild: emit the GA dataLayer events byte-identical and DO NOT add client-side `cvg` calls for them (would double-count). Verify GTM's Converge tags when convenient. | Owner-confirmed event set. Mechanism (GTM relay) to verify in GTM; non-blocking | RESOLVED (mechanism to verify) |
| 2b | Owner request: enrich completion/signup events with answer attributes (concern, age…) for Facebook targeting | Worker `converge.service.ts:46-63` spreads `...properties = payload.mixpanel` | **`Signed Up` ALREADY carries the answer attributes** (the cdp-mapped mixpanel props are spread into the Converge payload). The client-side `quiz started/completed` events currently carry only GA params (event_category, position, q_name/q_email), NOT answer attributes. | ENHANCEMENT for Phase 1: make the spec able to attach selected answer attributes to client events (quiz completed/started) for FB targeting; `Signed Up` already enriched. | RESOLVED (Phase 4 Task 2: `spec.eventEnrichment` now forwards `concern`/`age`/`goal` onto the answer / completion / checkout client events) |
| 3 | Live Flutter `redirect_to_checkout.dart:22,168` base `...save-85-5-59/` | Owner: that `-59` is from an OUTDATED TEST; canonical = `...save-85-5/` | Live checkout slug has a stale `-59` | RESOLVED — rebuild uses `...save-85-5/`. Owner also requires: KEEP the full URL-param logic — coupon tag derived from answer ids (make this CONFIGURABLE in the spec), billing prefill, and `__cvg_uid`/`__cvg_sid` session-stitching params. | RESOLVED |
| 4 | `redirect_to_checkout_scalp.dart` (`redirectToCheckoutScalp`) | Grep of `lib/` for call sites | `redirectToCheckoutScalp` has NO call sites — dead code (different base `...-85-5-37-copy-3/`, uses `session_id` not `__cvg_sid`) | Confirmed dead; MUST NOT be ported to the rebuild | RESOLVED |
| 5 | `cdpMapping` (`app_state.dart:1028,1046`) | — | `mindsetState` and `hairqare` both map to AC `field_56`; `hairqare`'s `mpField` is `mindsetState` (crossed naming) | Documented in cdp-coupon-map.md Table A as live behavior to replicate exactly | RESOLVED |
| 6 | quiz-flow.md idx 10 row (`Progression: auto`) | LIVE quiz + QC against template `question_answer_additionl_info_widget.dart:171-207` | Live idx 10 (shampoo spend) does NOT auto-advance: on tap the `AnimatedText.onAnswerSelected` (171-185) only records the answer + reveals the title/description block; the `FooterButtonWidget` Continue renders only `if (selctedValue != null)` (195-207) and its `answerAction` (home_page_widget.dart:632-663) holds the `nextPage` at :659. So idx 10 = reveal-then-Continue (`cta`). NB: the diet screen above it (idx 9) IS true auto (nextPage straight from answerAction, :602-621) — do not conflate | QC-CONFIRMED. Rebuild: idx 10 is `cta` with reveal text + explicit Continue | RESOLVED |
| 7 | quiz-flow.md idx 11 & 12 answer-option tables (`app_state.dart` hairMyth / hairDamageActivity — 5 options each) | LIVE quiz + QC against template `multi_choice_with_image_question_check_box_widget.dart:489-611` | The "None of the above" option is **template-injected** (hard-coded `InkWell` below the data ListView, localization key `1var0gwm`), NOT in the data lists. On tap (494-522) it clears all data checkboxes, sets `selectedValue=1000`, resets `selectedAnswer=[]`, appends the sentinel string **`'n/a'`**, fires `Question Answered`, and advances. (Unrelated to the `nonQuestionAnswerItem='not applicable'` constant used elsewhere.) | QC-CONFIRMED. Rebuild: both idx 11 & 12 need a "None of the above" that clears other picks, records answer `'n/a'`, and advances | RESOLVED |
| 8 | Phase 0 Task 5 golden capture (screens 0-18) | Screens 19 (dashboard/result), 20 (final pitch), floating timer | Result/dashboard (idx 19), final pitch (idx 20), and the floating countdown timer require a real email-form submission to reach (submit fires the production Worker → Mixpanel/AC/Converge and creates a live lead). They were NOT captured to avoid a fake production lead | **DEFERRED** pending a safe capture method: a staging URL, a local FlutterFlow export build run offline, or an owner-approved tagged/test submission (e.g. test email + suppressed downstream). Capture screens 19, 20 + floating timer once one of those is available | RESOLVED (Phase 4: live full-page dashboard reference captured via ONE owner-approved tagged test submission → `docs/reference/golden/19-dashboard-live.png`; React dashboard to be eyeballed against it) |
| 9 | `src/quiz/hairquiz.spec.ts` prompts (authored) | golden PNGs + `internationalization.dart` (en) + `home_page_widget.dart` | **9 question prompts in the spec are paraphrases/invented copy, not the live on-screen text.** idx0 "See if the Challenge is a fit…" vs "See if the Challenge is a fit for you and your hair profile"; idx1 "What is your hair type?" vs "Which hair type do you have?"; idx3 "What is your biggest hair concern?" vs "What is your biggest hair concern right now?"; idx4 "How would you describe your current haircare routine?" vs "What best describes your current haircare routine?"; idx7 "Are you familiar with the Hairqare method?" vs "How familiar are you with HairQare and our approach to holistic haircare?"; idx9 "How would you describe your diet?" vs "What best describes your diet?"; idx10 "How much do you usually spend on shampoo?" vs "How much do you spend on a bottle of shampoo?"; idx11 "Which of these do you believe?" vs "Which of these hair care myths do you believe?"; idx12 "Which of these do you do regularly?" vs "Select the damaging practices that you regularly do" | QC-FAIL. Spec must use the verbatim live prompts (keys `g2q0w5rd`/`uaeia9o3`/`15jpp8px`/`zaokmujb`/`m7uh7oex`/`khaadglv`/`25wr54y6`/`rihaqphi`/`ugf9xggr`; idx0 literal at `home_page_widget.dart:152-153`) | OPEN |
| 10 | `src/quiz/hairquiz.spec.ts` idx14/idx15 rating prompts | golden `14`/`15` PNGs + `home_page_widget.dart:1050,1094` + i18n `vazebtnl`/`zcjftwlh` | Spec idx14 prompt "My reflection in the mirror makes me feel…" should be **"My reflection in the mirror affects my mood and self-esteem."** (key `vazebtnl`); idx15 "I tend to compare my hair to others…" should be **"I tend to compare my hair to others' and it makes me frustrated."** (key `zcjftwlh`). The "How much do you relate to the following statement?" line is the template **subQuestion**, not the main prompt; the spec single-`prompt` model loses it. The rating `answerId`/`label` `'1'..'5'` ARE correct (`rating_question_options_widget.dart:210,288,366,444,522` record string `'1'..'5'` → CDP field_53/54). | OPEN |
| 11 | `src/quiz/hairquiz.spec.ts` idx16 prompt | golden `16` PNG + i18n `hoc1sdcs` | Spec "Has a professional ever recommended haircare to you?" should be **"Did a professional refer you to us?"** (key `hoc1sdcs`). | OPEN |
| 12 | `src/quiz/content/pitch-claim-matrix.ts` + spec conditional copy | `home_page_widget.dart` claim/description blocks (curly U+2019 apostrophes) | Minor: Flutter source uses curly apostrophes (`you've`, `you're`); the authored TS uses straight `'`. quiz-flow.md golden tables also transcribe straight, so the spec faithfully reproduces quiz-flow.md — flagged for byte-exactness only, not a logic defect. Claim 5×5 structure, concern phrases, routine clauses, description first-match groups, and resolution/knowledge tables otherwise match quiz-flow.md verbatim. | NOTE |
| 8b | Phase 3 Task 8 visual/content-parity gate (`e2e/visual.spec.ts`) | `docs/reference/golden/*.png` (LIVE FLUTTER renderer) | The golden PNGs are from the live **Flutter** quiz — a different renderer (fonts/layout/scaling differ), so pixel-diffing the React app against them would always fail and is NOT done. Instead the React app's screens 0–18 are **content-verified** (expected prompts/sub-instructions + answer-option labels asserted), the **no-scroll fit-to-viewport invariant** is asserted on every non-result screen, the React app's **OWN** Playwright snapshot baseline is committed under `e2e/visual.spec.ts-snapshots/` (guards future React changes, NOT compared to Flutter), and a human-review folder `e2e/review/NN-<id>.react.png` (gitignored, regenerated each run) is emitted for side-by-side eyeballing vs `golden/NN-*.png`. The **dashboard/result (idx 19) is content-verified structurally** (user name, match-% number, benefit text, CTA button, countdown timer present; allowed to scroll) — its **PIXEL visual remains DEFERRED per ISSUES #8** pending a safe Flutter-build or owner-approved test capture. | DONE (pixel still DEFERRED, see #8) |

---

## Phase 4 close-out — residual ledger

Reconciliation of the Phase-3 residuals against what Phase 4 (Tasks 1–8) actually
shipped. Each is marked **RESOLVED** (with how) or **DEFERRED** (with why). Source:
`docs/reference/qa-report.md` + the Phase 4 commits on `phase-4-polish`.

| Residual | Resolution | Status |
|----------|------------|--------|
| **idx-10 reveal-then-Continue** (shampoo-spend records + reveals, then advances on explicit Continue) | Phase 4 Task 1: the store honors `progression: 'cta'` + reveal — tap records the answer and reveals the title/description block; `continue()` fires `Question Answered` and advances. Matches live (ISSUES #6). | RESOLVED |
| **Dashboard pixel-visual capture** (ISSUES #8) | Phase 4 captured the **live full-page reference** `docs/reference/golden/19-dashboard-live.png` via ONE owner-approved tagged test submission. The React dashboard is to be eyeballed against it (no automated pixel diff — different renderer per ISSUES #8/#8b). | RESOLVED |
| **SkipModal copy** (was "Stay" / "Skip anyway") | Phase 4 Task 3: live copy now used — heading **"⚠️ Before you continue…"**, buttons **"BACK TO QUIZ"** / **"SKIP QUIZ"**. | RESOLVED |
| **Match-% random vs answer-derived** | Owner decision **D1: keep random 92–97** (`randomInteger(92, 97)`, seeded once into `resultPercentage`). Faithful to the Flutter original; not made answer-derived. | RESOLVED (by D1 decision) |
| **Legacy CDN asset URLs** (`assets.hairqare.co`, `pub.hairqare.co`, `uploads-ssl.webflow.com`) | Phase 4 Task 6: all **38 image URLs verified HTTP 200** (link-check in qa-report.md). Owner decision **D3: keep the legacy assets** (real brand assets) — no replacements. | RESOLVED (by D3 decision) |
| **Avatar age-bracket mismatch** (SVG filenames `Under 18`/`25-34`/`35-44`/`45+` vs answer brackets `18-29`/`30-39`/`40-49`/`50+`) | Phase 4 Task 3: verified our mapping is **byte-identical to the live quiz** — the live quiz uses the same filename→bracket mapping, so there is **no gross mismatch vs production**; replicated as-is. | RESOLVED |
| **eventEnrichment not forwarded to client events** (ISSUES #2b) | Phase 4 Task 2: `spec.eventEnrichment` now forwards **concern / age / goal** onto the answer, completion, and checkout client events (for FB targeting). `Signed Up` was already enriched server-side. | RESOLVED |
| **FitViewport landscape clipping** (NEW — Phase 4 Task 7) | Short 740×360 landscape clipped the tallest screen (`hairMyth` multi-select) off-viewport and untappable. Fixed in `src/components/layout/FitViewport.tsx`: lowered scale floor `0.6`→`0.45`, top-align (`justify-start`) whenever `scale < 1`, +8px height safety margin. All 4 device viewports now pass no-scroll + tap-target. | RESOLVED |
| **Reckless Neue headline font** (NEW — main visual gap vs live brand) | The real **Reckless Neue** headline font files are **not in the brand repo** (`~/github/figma-holiniq-brand/`). Headlines currently fall back to a serif stack (`'Reckless Neue', Georgia, 'Times New Roman', serif`). Swap in the real `@font-face` files when they become available. | **DEFERRED** (font files unavailable) |

**Owner cleanup TODO:** the QA test lead used for the dashboard capture —
`qa.dashboard.test@holinic.co` — should be **deleted from ActiveCampaign, Mixpanel,
and Converge** by the owner (it was a tagged/suppressed submission, but the contact +
events still exist downstream).
